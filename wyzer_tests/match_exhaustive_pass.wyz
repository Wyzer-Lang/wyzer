fn process(r: Result<u32, str>) -> u32 {
    match r {
        Ok(v) => v,
        Err(e) => 0u32
    }
}

fn main() {
    let r: Result<u32, str> = Ok(42u32);
    let res: u32 = process(r);
    std::io::println(res);
}
