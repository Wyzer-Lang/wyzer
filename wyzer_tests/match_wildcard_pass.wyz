fn process(r: Result<u32, str>) -> u32 {
    match r {
        Ok(v) => v,
        _ => 0
    }
}

fn main() {
    let r: Result<u32, str> = Err("Network Error");
    let res: u32 = process(r);
    std::io::println(res);
}
