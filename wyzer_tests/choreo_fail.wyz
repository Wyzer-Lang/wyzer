role @Client;
role @Server;

global SECRET: u32@Server = 42;

fn print_secret() @ Client {
    // Should fail because SECRET is a Server variable but we are in Client!
    let data = SECRET;
}

fn main() { }
